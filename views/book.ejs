<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="../Css/book.css">
    <title>Document</title>
</head>
<body>
    <div class="nav-container">
        <ul>
            <li><img class="logo"src="./pic/logo.png" width="50px" height="50px ></li>
            <li><img src="#" alt=""></li>
            <li><a href="/index">Home</a></li>
            <li><a href="/book">Books</a></li>
            <li><a href="/contact">Contact</a></li>
            <li><a href="/news">News</a></li>
            <!-- เปลี่ยนตำแหน่งของลิงก์ Login และ Register -->
            <li class="profile-btn"><a href="/profile/<%= userid %>"><%= username %></a> </li>
            <form action="/logout" method="POST">
                <button class="btn-logout"type="submit">Logout</button>
            </form>
        </ul>

        <center><h3 class="title">COZYBOOKS REVIEWS.</h3></center>
    <div class="book-container" id="bookContainer">
        <div class="book-list">
            <% books.forEach((book, index) => { %>
                <div class="book-images">
                    
                        <div class="type">Title: <%= book.title %></div> <!-- แสดงประเภทหนังสือ -->
                        <a href="/comment/<%= book.bookid %>">
                        <img src="<%= book.image %>" alt="<%= book.title %>" />
                        </a>
                        <div class="author">Author: <%= book.author %></div><br>
                       
                
                    </a>
                </div>
                <% if ((index + 1) % 5 === 0) { %> <!-- เพิ่มแถวใหม่ทุก 5 รายการ -->
                    </div><div class="book-list">
                <% } %>
            <% }); %>
        </div>
    
        <footer>
            <h3>2024 COZYBOOKS.COM</h3>
        </footer>
        <a id="scroll-btn" href="#section-2"></a>
        
 
    </div>
    <br>
    <style>
        /* width */
        ::-webkit-scrollbar {
          width: 15px;
        }
        
        /* Track */
        ::-webkit-scrollbar-track {
          box-shadow: inset 0 0 5px grey; 
          border-radius: 10px;
        }
         
        /* Handle */
        ::-webkit-scrollbar-thumb {
          background: rgb(77, 76, 76); 
          border-radius: 10px;
        }
        
        /* Handle on hover */
        ::-webkit-scrollbar-thumb:hover {
          background: #303030; 
        }
        </style>
    <!-- <img src="./pic/HarryPotter.jpg"/>
    <img src="./pic/Lordofthering.jpg"/> -->
</body>
</html>